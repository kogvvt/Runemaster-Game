public class Player extends Actor{
    public Player(World world, char character, int maxHp, int attack, int defense, String name) {
        super(world, character, maxHp, attack, defense, name);
    }
}
